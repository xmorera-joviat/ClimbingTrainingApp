<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClimbingTrainingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <!-- Estil per als botons del grup 1 (marró) -->
    <style name="RoundBrownButton">
        <item name="android:background">@drawable/round_brown_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Estil per als botons del grup 2 (verd) -->
    <style name="RoundGreenButton">
        <item name="android:background">@drawable/round_green_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Estil per als botons del grup 3 (vermell) -->
    <style name="RoundRedButton">
        <item name="android:background">@drawable/round_red_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>